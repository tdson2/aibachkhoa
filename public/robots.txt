# https://aibachkhoa.com/robots.txt

User-agent: *
Allow: /

# Installer binaries: no value in a search index, and crawling them burns
# crawl budget on tens of megabytes.
Disallow: /downloads/

# The analytics dashboard and its API. Both already refuse anyone without
# the password and answer with X-Robots-Tag: noindex; this is the polite
# version of the same thing, so crawlers never ask.
Disallow: /admin
Disallow: /api/

Sitemap: https://aibachkhoa.com/sitemap.xml
